# Home Assistant add-on repository manifest.
# https://developers.home-assistant.io/docs/add-ons/repository/
#
# This is the public-facing add-on repository for NeoHelio Satellite. Once
# pushed to a public GitHub repo, customers add the URL via:
#   Settings → Apps → ⋮ → Repositories → paste URL → Add
# HA Supervisor then picks up every sub-directory as an installable add-on.
name: NeoHelio Add-ons
url: https://github.com/neohelio/satellite-addon
maintainer: NeoHelio Pty Ltd <hello@neohelio.io>
